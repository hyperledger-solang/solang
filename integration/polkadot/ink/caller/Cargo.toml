[package]
name = "caller"
version = "0.1.0"
authors = ["Cyrill Leutwiler <cyrill@parity.io>"]
edition = "2021"

[dependencies]
ink = { version = "5.0.0", default-features = false }

[lib]
path = "lib.rs"

[features]
default = ["std"]
std = [
    "ink/std",
]

[workspace]
